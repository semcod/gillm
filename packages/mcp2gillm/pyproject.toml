[build-system]
requires = ["setuptools>=77", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "mcp2gillm"
version = "0.1.0"
description = "MCP stdio server for dsl2gillm"
readme = "README.md"
requires-python = ">=3.10"
license = "Apache-2.0"
dependencies = [
    "dsl2gillm>=0.1.0",
    "nlp2gillm>=0.1.0",
    "mcp>=1.0,<2",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
mcp2gillm = "mcp2gillm.cli:main"

[tool.setuptools]
package-dir = { "" = "src" }

[tool.setuptools.packages.find]
where = ["src"]

[tool.uv.sources]
dsl2gillm = { workspace = true }
nlp2gillm = { workspace = true }

[tool.pytest.ini_options]
testpaths = ["tests"]
