{
  "$id": "dsl2gillm/commands/capture",
  "type": "object",
  "required": ["verb"],
  "properties": {
    "verb": { "const": "CAPTURE" },
    "scale": { "type": "number", "minimum": 0.01, "maximum": 1.0, "default": 0.2 }
  },
  "additionalProperties": false
}
